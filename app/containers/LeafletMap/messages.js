/*
 * LeafletMap Messages
 *
 * This contains all the text for the LeafletMap container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LeafletMap';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LeafletMap container!',
  },
});
